#include "main.h"
/**
 * mul - funccnction that multiplies two integers
 * @a: first input to multiply
 * @b: second input to multiply
 * Return: multiplication of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
